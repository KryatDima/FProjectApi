@model FProjectMVC.Models.BrandViewModel

@{
    ViewData["Title"] = "GetBrandById";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


    <div>
        <div><h4>Title: @Model.Title</h4></div>
        <div>Description: @Model.Description</div>
        <button type="submit">@Html.ActionLink("Edit", "Update", "Brand", new { id = Model.Id }, null)</button>
        <button type="submit">@Html.ActionLink("Delete", "Delete", "Brand", new { id = Model.Id }, null)</button>
    </div>

